[project]
name = "llm-brain"
version = "0.1.0"
description = "LLM Brain - ATS Resume & LaTeX Generation Engine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.139.2",
    "httpx>=0.28.0",
    "langchain>=1.3.14",
    "langchain-groq>=1.1.3",
    "langchain-openai>=0.3.0",
    "python-dotenv>=1.1.0",
    # Read-only PDF inspection: page count and last-page text extent, used to
    # measure whether the generated CV lands on 1 / 1.5 / 2 pages. Pure Python.
    "pypdf>=5.1.0",
    "uvicorn>=0.51.0",
]
